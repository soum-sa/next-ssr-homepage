import { css } from '@panda-styled-system/css';

export const loginButton = css({
  color: 'white',
  backgroundColor: 'blue-base',
  paddingInline: 12,
  minBlockSize: 12,
  borderRadius: 5,
  display: 'inline-flex',
  alignItems: 'center',
  fontWeight: 'bold',
  fontSize: '13',
});
